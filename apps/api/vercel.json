{
  "version": 2,
  "installCommand": "cd ../.. && npm install && npx prisma generate --schema=packages/db/prisma/schema.prisma",
  "builds": [
    {
      "src": "src/handler.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/handler.ts"
    }
  ]
}
